import { createStore } from 'vuex'
import loginModule from './login'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    login: loginModule
  }
})
